apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    run: maliev-currencyservice-api
  name: maliev-currencyservice-api
  namespace: maliev
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    name: http
  selector:
    run: maliev-currencyservice-api
  sessionAffinity: None
  type: NodePort
